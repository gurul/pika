#ifndef ELEGOO_ESP32_CAMERA_SECRETS_H
#define ELEGOO_ESP32_CAMERA_SECRETS_H

// Copy this file to secrets.h and set your home Wi‑Fi (2.4 GHz band required for ESP32).
// secrets.h is gitignored — do not commit real credentials.

#define HOME_WIFI_SSID "your-2.4ghz-ssid"
#define HOME_WIFI_PASS "your-wifi-password"

#endif
